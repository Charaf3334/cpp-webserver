- handle missing of required fields and errors to throw (ie. root and index and so on)
- handle when no config file is passed as parameter, should work with a default one present in some PATH

- khsni mzl nchof subject lakant chi haja tzad f config file w ha7na salina hh

- 444 closes connection.

to do:
    - server_name khso yt7yd wa9ila ✅
    - its okay for two servers to listen to same port mais khs ykon 3ndhum different virtual host name (aka server name), mais 7na mo7al ndiroha ✅
    - fix listen syntax (listen 127.0.0.1:8080) ✅

    - redirection return [CODE] [URL], return [CODE] [TEXT] ✅, return [URL] (code howa 302) ❌
    - for redirection i still need to check for codes and if the next argument is TEXT or URL, since some status codes only support TEXT and others only support URL; ❌

    - need to add upload_dir directive as PATH to store uploaded files. ❌

    - nginx allows multiple same location, mais kayoverridi lakano 2 /api aykhdm b akhir whda. ❌
    - check for all directives and their behavior using nginx ❌

    - content-type will be stored as std::map (extension, content-type) ❌
    - status codes and their messages will be stored as std::map (code, message) ❌









    ./auto/configure --prefix=$HOME/nginx
    make; make install





    errors i found f request parsing:
        when value has spaces, we treat it as error when it shouldn't be error

    tests that shouldn't pass but pass for now: 
    - printf "GET / HTTP/1.1\r\r\n" | nc localhost 8080
    - printf "GET / HTTP/1.1\r\n\n" | nc localhost 8080
    - printf "GET / HTTP/1.1\r\n\r" | nc localhost 8080
    - printf "GET / HTTP/1.1\n\r\n" | nc localhost 8080
    - printf "GET /\r HTTP/1.1\r\n" | nc localhost 8080

    duplicate in headers, check for host in http.1.1

    GET / HTTP/1.1
    Host: 127.0.0.1:8080
    Connection: keep-alive
    Cache-Control: max-age=0
    sec-ch-ua: "Chromium";v="142", "Brave";v="142", "Not_A Brand";v="99"
    sec-ch-ua-mobile: ?0
    sec-ch-ua-platform: "Linux"
    Upgrade-Insecure-Requests: 1
    User-Agent: Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/142.0.0.0 Safari/537.36
    Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8
    Sec-GPC: 1
    Accept-Language: en-US,en;q=0.8
    Sec-Fetch-Site: none
    Sec-Fetch-Mode: navigate
    Sec-Fetch-User: ?1
    Sec-Fetch-Dest: document
    Accept-Encoding: gzip, deflate, br, zstd


    HTTP/1.1 KEEPS CONNECTION ALIVE
    HTTP/1.0 CONNECTION CLOSE

    - serviw error pages
    
    -   printf "GET / HTTP/1.0\r\n\r\n" | nc localhost 8080
        HTTP/1.0 400 Bad Request
        Content-Type: text/plain
        Content-Length: 19
        Connection: close

        Error: Bad Request

movie stream dynamic

charaf:
    tibarike: "GET / HTTP/1.0\r\nzakaria:  hh \r\n\r\n" spaces after dopwan 


    zakaria - cgi
    i should build a default working cgi files in cgi files final
    exit status error page
    error page
    ✅query params
    ✅environment in cgi
    ✅register/login pirate stream
    
    ✅: decodeURI: /autoindex/upload/Client_10/hair%20wrap.png -> GET hair wrap.png -> is this correct :
        uri before: /autoindex/upload/Client_10/hair%20wrap.png%3Fusername=test
        request.queries: ?username=test
        request.uri: /autoindex/upload/Client_10/hair wrap.png


l9raya
testat


url enconding, browser kisendi special characters(space, <, >, ", #, %, {, }, |, \, ^, ~, [, ], `) as % encoding, o7naya kandecodiwhom, saad lli ay3tina ras lkhit

movie stream dynamic


DEFENSE:
    -why didn't you handle absolute uri in your server? According to the rfc, absolute uri is only allowed when the request is being made to a proxy. And this is not a proxy, this is a werbserver you piece of shit | https://datatracker.ietf.org/doc/html/rfc1945#section-5.1.2
